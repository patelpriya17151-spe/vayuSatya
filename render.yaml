services:
  - type: web
    name: vayusatya-backend
    runtime: python311
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn -w 4 -b 0.0.0.0:$PORT wsgi:app"
    envVars:
      - key: OWM_API_KEY
        scope: run
        sync: false
